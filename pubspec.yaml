name: flutter_graphql_custom_channel
description: for graphql issue related to WebSocketChannel interaction
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.16.2 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  graphql: 5.1.2-beta.5
  http: ^0.13.4
  logging: ^1.0.2
  web_socket_channel: ^2.2.0
  stream_channel: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true
